
* Via * pip * :
    :

    $> pip install - f http:
        //repodipory.u - strasbg.fr / lib / python pydiploy == <version >

* Depuis les sources:
    :

    $> wget http:
        //repodipory.u - strasbg.fr / lib / python / pydiploy - <version > .tar.gz
    $> tar xvf pydiploy - <version > .tar.gz
    $> cd pydiploy - <version >
    $> python setup.py install
